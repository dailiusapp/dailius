import type { Metadata } from "next";
import { ComingSoon } from "@/components/app/ComingSoon";

export const metadata: Metadata = { title: "Profile — Dailius" };

export default function ProfilePage() {
  return <ComingSoon title="Profile" />;
}
